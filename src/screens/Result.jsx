import { CSS_ANIMATIONS, COLORS, MOODS, styles } from "../constants";

export default function Result({ preMood, postMood, aiMsg, aiLoading, todayCount, totalMins, onDone }) {
  return (
    <div style={styles.page}>
      <style>{CSS_ANIMATIONS}</style>
      <div style={styles.box} className="pop">

        <div style={{ ...styles.card, textAlign: "center", border: "1px solid var(--app-teal-a40)" }}>
          <div style={{ fontSize: 48, marginBottom: 20 }}>✨</div>

          {/* Mood journey */}
          <div style={{ display: "flex", alignItems: "center", justifyContent: "center", gap: 20, marginBottom: 28 }}>
            <div style={{ textAlign: "center" }}>
              <div style={{ fontSize: 36 }}>{MOODS[preMood]}</div>
              <div style={{ color: COLORS.MUTED, fontSize: 11, marginTop: 6 }}>Before</div>
            </div>
            <div style={{ color: COLORS.TEAL, fontSize: 22 }}>→</div>
            <div style={{ textAlign: "center" }}>
              <div style={{ fontSize: 36 }}>{MOODS[postMood]}</div>
              <div style={{ color: COLORS.MUTED, fontSize: 11, marginTop: 6 }}>After</div>
            </div>
          </div>

          {/* AI message */}
          <div style={{
            background: COLORS.SURF2,
            border: "1px solid var(--app-teal-a15)",
            borderRadius: 16, padding: "18px 20px",
            marginBottom: 20, minHeight: 72,
            display: "flex", alignItems: "center", justifyContent: "center",
          }}>
            {aiLoading ? (
              <div style={{ color: COLORS.MUTED2, fontSize: 13 }} className="pulse">FocusBuddy is thinking...</div>
            ) : (
              <div style={{ color: COLORS.LITE, fontSize: 14, lineHeight: 1.7, fontStyle: "italic" }}>"{aiMsg}"</div>
            )}
          </div>

          <div style={{ color: COLORS.MUTED, fontSize: 11, marginBottom: 20 }}>
            — FocusBuddy AI (powered by Claude)
          </div>

          {/* Stats */}
          <div style={{ display: "flex", justifyContent: "center", gap: 28, marginBottom: 24 }}>
            {[["Today", todayCount, "sprints"], ["Total", totalMins, "minutes"]].map(([label, val, unit]) => (
              <div key={label} style={{ textAlign: "center" }}>
                <div style={{ color: COLORS.MUTED, fontSize: 11, marginBottom: 2 }}>{label}</div>
                <div style={{ color: COLORS.TEAL, fontSize: 24, fontWeight: 800, lineHeight: 1 }}>{val}</div>
                <div style={{ color: COLORS.MUTED, fontSize: 10, marginTop: 2 }}>{unit}</div>
              </div>
            ))}
          </div>

          <button style={styles.primaryBtn} onClick={onDone}>Back to Dashboard</button>
        </div>

        <p style={{ color: COLORS.MUTED, fontSize: 11, textAlign: "center", marginTop: 10 }}>
          AI responses generated by Anthropic Claude · not medical advice
        </p>

      </div>
    </div>
  );
}
